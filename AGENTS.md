# 커밋 메시지 규칙

사용자 요청에 따라 이 프로젝트의 커밋 메시지는 아래 문서를 기준으로 작성한다.

- 형식: `타입: 제목` — 타입 뒤에 괄호나 스코프를 붙이지 않는다.
- 타입은 `feat`, `fix`, `build`, `chore`, `ci`, `docs`, `style`,
  `refactor`, `test`, `perf` 중 변경 목적에 맞게 선택한다.
- 제목은 50글자 이내로 작성하고 끝에 마침표를 붙이지 않는다.
- 제목은 과거형 대신 명령형으로 작성한다. 한국어는 `추가`, `수정`, `제거`
  등의 표현을 사용한다. 영어 제목의 첫 글자는 대문자로 쓰되 타입은 소문자로 유지한다.
- 제목·본문·바닥글 사이는 빈 줄로 구분한다.
- 본문은 각 행을 72글자 이내로 작성하고, 무엇을 왜 변경했는지 설명한다.
- 제목만으로 충분하면 본문을 생략한다.
- 바닥글은 실제 관련 이슈 등 참조 정보가 있을 때만 작성한다. 이슈 번호를 지어내지 않는다.
- 별도 언어 요청이 없으면 제목과 본문은 한국어로 작성한다.

예시:

```text
feat: Windows 알림 선택과 테스트 기능 추가

다른 앱 사용 중에도 입출차를 확인할 수 있도록 Windows 알림을 지원한다.
설정에서 웹 알림과 Windows 알림을 선택하고 테스트할 수 있게 한다.
```
