# 버전 고정 — 업데이트 시 사용자 PC 에서 pip install -r 이 실행되므로,
# 범위로 두면 그 시점의 최신 패키지가 들어와 예고 없이 동작이 바뀔 수 있다.
# 올릴 때는 개발 PC 에서 먼저 검증한 뒤 이 파일의 버전을 함께 수정할 것.
requests==2.34.0
certifi==2026.4.22
fastapi==0.137.1
uvicorn[standard]==0.49.0
jinja2==3.1.6
python-multipart==0.0.32
pystray==0.19.5
pillow==12.2.0
